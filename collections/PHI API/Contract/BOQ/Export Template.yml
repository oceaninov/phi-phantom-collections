info:
  name: Export Template
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/api/v1/contracts/boqs/template?type=4"
  params:
    - name: type
      value: "4"
      type: query
      description: "type options : 1, 2, 3, 4"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
