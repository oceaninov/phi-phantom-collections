info:
  name: Read All
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/v1/notifications/read-all"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: success
    request:
      url: "{{base_url}}/api/v1/notifications/read-all"
      method: GET
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json; charset=UTF-8
        - name: Vary
          value: Origin
        - name: Date
          value: Wed, 03 Jan 2024 03:41:17 GMT
        - name: Content-Length
          value: "105"
      body:
        type: json
        data: |-
          {
              "process_time": "257ms",
              "status": "success",
              "code": "",
              "message": "Mark all notifications as read succeed"
          }
