info:
  name: Get by Name
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/v1/custom-configurations/name/usd_rate"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
