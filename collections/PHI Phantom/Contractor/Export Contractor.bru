meta {
  name: Export Contractor
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/contractors/export?sort_column=created_at&sort_order=DESC
  body: none
  auth: bearer
}

params:query {
  sort_column: created_at
  sort_order: DESC
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
}
