info:
  name: Get Entries
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/cronjobs/entries"
  auth:
    type: basic
    username: "{{basic_auth_username}}"
    password: "{{basic_auth_password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
